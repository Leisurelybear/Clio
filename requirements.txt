# Core dependencies (loose pins, compatible with existing envs)
# For exact reproducible builds, use requirements-locked.txt
google-genai>=2.7.0
httpx[socks]>=0.28.1
pyyaml>=6.0.3

# Speech transcription: see requirements-whisper.txt
