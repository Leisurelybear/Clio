"""Route handler package for the UI server.

Each module exports handler functions called from server.py's do_GET/do_POST/do_PUT.
The first positional parameter is always the Handler instance (self).
"""
